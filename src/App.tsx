import React from "react";
import "./index.css"; /* Is this necessary? Or is it enough to have imported it in index.tsx? */
import Form from "./Form";

function App() {
  return <Form />;
}

export default App;
